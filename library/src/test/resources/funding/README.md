Fixtures are response examples from [Kraken’s Spot REST OpenAPI specification](https://docs.kraken.com/openapi/spot-rest.yaml), retrieved on 2026-09-08. Supplemental inline cases cover paginated status responses, missing and unlimited deposit limits, precise amounts, rate limit windows, enum fallback, and unsuccessful cancellation. The DepositStatus schema uses a singular `deposit` field while the unpaginated example is an array; status parsers accept singular and plural collection fields. All tests run locally without API keys, network calls, or funding actions.
