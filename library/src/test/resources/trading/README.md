Fixtures are response examples from [Kraken’s Spot REST OpenAPI specification](https://docs.kraken.com/openapi/spot-rest.yaml), retrieved on 2026-09-21. AddOrder, AddOrderBatch and EditOrder use the documented examples with a conditional close order, the richest ones available. Supplemental inline cases cover validation-only orders without transaction IDs, partially rejected batches, a disabled dead man's switch returning a `0` trigger time, pending cancellations, and case-insensitive or unknown edit statuses. Request bodies for AddOrderBatch and CancelOrderBatch follow the documented JSON examples, where `orders` holds order objects and plain identifiers respectively. All tests run locally without API keys or network calls, and place, amend or cancel no orders.
