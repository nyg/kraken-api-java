Fixtures are response examples from [Kraken’s Spot REST OpenAPI specification](https://docs.kraken.com/openapi/spot-rest.yaml), retrieved on 2026-09-08. The redundant `response` wrapper in the OrderAmends documentation example is removed to match the declared response envelope. The API key example is replaced with a test placeholder. Supplemental inline cases cover nullable credit results, fee schedules, unknown enum values, and margin order flags. Tests make no network requests.

The signing tests use the public example secret and AddOrder vector in [Kraken authentication documentation](https://docs.kraken.com/exchange/guides/rest/authentication), never account credentials. The Balance and TradeVolume reference digests were computed independently with Python hashlib and hmac from that public example secret, the endpoint path and the exact nonce/body bytes. These tests are offline and do not place orders or access an account.
